{
  "name": "anteon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/tokens-md.mjs && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "tokens:md": "node scripts/tokens-md.mjs"
  },
  "dependencies": {
    "lenis": "^1.1.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "typescript": "^5.8.2",
    "vite": "^6.3.5"
  }
}
