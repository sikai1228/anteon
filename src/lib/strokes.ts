/** Stub. render-core replaces this file per SPEC.md. */

import type { StrokeSetApi, StrokeSetOptions } from './types';

export function createStrokeSet(_opts: StrokeSetOptions): StrokeSetApi {
  throw new Error('strokes.ts is a stub; render-core implements it');
}
